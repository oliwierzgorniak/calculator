import { StyleSheet } from "react-native";
import colors from "../../js/colors";

export default StyleSheet.create({
  numbersContainer: { flex: 7, backgroundColor: colors.numbers },
  numbersRow: {
    flexDirection: "row",
    flex: 1,
  },
});
